services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./gallery.db:/app/gallery.db
      - ./uploads:/app/uploads
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped